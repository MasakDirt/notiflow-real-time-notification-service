server:
  port: 8070

spring:
  application:
    name: telegram_kafka
  kafka:
    bootstrap-servers: localhost:29092
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true